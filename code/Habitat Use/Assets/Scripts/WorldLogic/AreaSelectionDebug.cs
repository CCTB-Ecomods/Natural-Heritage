using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;

public class AreaSelectionDebug : MonoBehaviour
{
    /*public List<TileData> area = new List<TileData>();

    private void Update()
    {
        if (Input.GetKeyDown(KeyCode.A))
        {
            Debug.Log("a pressed");
            GameObject obj = (GameObject)Selection.activeObject;
            
            if(obj.GetType() == typeof(GameObject))
            {
                area = obj.GetComponent<TileData>().area.GetTileList();
                Debug.Log("selected area count: " + area.Count);
            }
        }
        if (Input.GetKeyDown(KeyCode.M))
        {
            foreach (var a in area)
            {
                a.gameObject.SetActive(false);
            }
        }

        if (Input.GetKeyDown(KeyCode.Z))
        {
            foreach (var a in area)
            {
                a.gameObject.SetActive(true);
            }
        }
    }*/
}
